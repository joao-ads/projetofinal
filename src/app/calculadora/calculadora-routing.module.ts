import { Routes } from "@angular/router";
import { CalculadoraComponent } from "./services";

export const CalculadoradRoutes: Routes = [
    {
        path: 'calculadora',
        component: CalculadoraComponent
    }
];